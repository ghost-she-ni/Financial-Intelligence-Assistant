# Required for OpenAI-hosted models or compatible gateways.
OPENAI_API_KEY=

# Optional: set this if you use an OpenAI-compatible endpoint or local proxy.
OPENAI_BASE_URL=

# Optional: live API smoke tests stay skipped unless this is set to 1.
RUN_LIVE_LLM_TESTS=0
